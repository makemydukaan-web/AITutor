# Ensure optional dependencies (like native modules) are installed
optional=true

# Platform-specific binaries
platform-arch-map=true

# Don't ignore scripts (needed for native modules)
ignore-scripts=false
